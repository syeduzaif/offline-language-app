// All endpoint paths in the app, grouped by backend.
// Services reference these constants instead of inlining path strings.

class TranslationEndpoints {
  static const String translate = '/get';
}
